# This file has been auto-generated by i3-config-wizard(1).
# i3 config file (v4)
# http://i3wm.org/docs/userguide.html

#
# Settings
#
set $mod Mod4
set $term urxvtc
force_display_urgency_hint 1000 ms
focus_follows_mouse no

# floating_modifier $mod

client.focused          #0088CC #0088CC #ffffff #dddddd
client.focused_inactive #333333 #333333 #888888 #292d2e
client.unfocused        #333333 #333333 #888888 #292d2e
client.urgent           #2f343a #900000 #ffffff #900000

# Bitstream comes from chromium packet
font pango: Bitstream Vera Sans, FontAwesome 8


# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

#
# Keybinds
#
bindsym $mod+Return     exec --no-startup-id $term
bindsym $mod+BackSpace  exec --no-startup-id $term -e /bin/zsh -c "ranger ~" # URxvt
#bindsym $mod+BackSpace  exec --no-startup-id $term -e "/bin/zsh -c 'ranger ~'" # Gnome-term
bindsym $mod+d          exec --no-startup-id dmenu_run
bindsym Print           exec --no-startup-id scrot -e 'mv $f ~/Pictures/screenshots/'
bindsym $mod+u          exec devmon --unmount-removable

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
#bindsym $mod+Left focus left
#bindsym $mod+Down focus down
#bindsym $mod+Up focus up
#bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
#bindsym $mod+Shift+Left move left
#bindsym $mod+Shift+Down move down
#bindsym $mod+Shift+Up move up
#bindsym $mod+Shift+Right move right

# split direction
#bindsym $mod+h split h
#bindsym $mod+v split v

# toggle fullscreen
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# toggle border
bindsym $mod+b          border toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# reload the configuration file
bindsym $mod+Shift+c reload

# restart (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# Laptop keybinds
bindsym $mod+F3     exec --no-startup-id zenbook-lights -k -d
bindsym $mod+F4     exec --no-startup-id zenbook-lights -k -u
bindsym $mod+F5     exec --no-startup-id zenbook-lights -s -d
bindsym $mod+F6     exec --no-startup-id zenbook-lights -s -u
bindsym $mod+F10    exec --no-startup-id pactl set-sink-mute 1 toggle
bindsym $mod+F11    exec --no-startup-id pactl set-sink-volume 1 -5%
bindsym $mod+F12    exec --no-startup-id pactl set-sink-volume 1 +5%

#
# Workspace
#
set $tag1 1
set $tag2 2
set $tag3 3
set $tag4 4
set $tag5 5
set $tag6 6
set $tag7 7
set $tag8 "8:www"
set $tag9 "9:virt"
set $tag10 "10:log"

bindsym $mod+1 workspace $tag1
bindsym $mod+2 workspace $tag2
bindsym $mod+3 workspace $tag3
bindsym $mod+4 workspace $tag4
bindsym $mod+5 workspace $tag5
bindsym $mod+6 workspace $tag6
bindsym $mod+7 workspace $tag7
bindsym $mod+8 workspace $tag8
bindsym $mod+9 workspace $tag9
bindsym $mod+0 workspace $tag10

bindsym $mod+Shift+1 move container to workspace $tag1
bindsym $mod+Shift+2 move container to workspace $tag2
bindsym $mod+Shift+3 move container to workspace $tag3
bindsym $mod+Shift+4 move container to workspace $tag4
bindsym $mod+Shift+5 move container to workspace $tag5
bindsym $mod+Shift+6 move container to workspace $tag6
bindsym $mod+Shift+7 move container to workspace $tag7
bindsym $mod+Shift+8 move container to workspace $tag8
bindsym $mod+Shift+9 move container to workspace $tag9
bindsym $mod+Shift+0 move container to workspace $tag10

# Put some autostarted applications in specific workspaces
#assign [class="Skype"] $tag9
#assign [class="VirtualBox"] $tag10
#assign [window_role="systemd-logg"] $tag10
#for_window [window_role="systemd-logg"] move workspace number $tag10
for_window [instance="systemd-logg"] move workspace number $tag10

#
# statusbar
#
bar {
	position top
    workspace_buttons yes
    status_command python ~/.i3/i3status.py
	#status_command $HOME/.conky/.conky_init.sh

    colors {
        background  #222222
        statusline  #dddddd
        separator   #666666
        focused_workspace   #0088CC #0088CC #ffffff
        active_workspace    #333333 #333333 #888888
        inactive_workspace  #333333 #333333 #888888
        urgent_workspace    #2f343a #900000 #ffffff
    }
}

#
# Scratchpads
#

#bindsym $mod+n  exec --no-startup-id test -z $(xdotool search --class vimNotes) && gnome-terminal -e "/bin/zsh -c 'xdotool getactivewindow set_window --class vimNotes && /bin/zsh'"; [class="vimNotes"] scratchpad show
bindsym $mod+n exec --no-startup-id test -z $(xprop -name vimNotes) && urxvtc -name vimNotes; [instance="vimNotes"] scratchpad show
bindsym $mod+p  exec --no-startup-id test -z $(xprop -name "Database.kdbx - KeePassX") && keepassx; [class="Keepassx"] scratchpad show

bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

#for_window [class="Gnome-terminal" class="vimNotes"] move scratchpad, scratchpad show
for_window [instance="vimNotes"] move scratchpad, scratchpad show
for_window [class="Keepassx"] move scratchpad, scratchpad show

#
# Windows to NOT tile
#
for_window [class="Pavucontrol"] floating enable

#
# Borders
#
new_window  none
new_float   1pixel

# Borderless windows
#for_window [class="Chromium"] border none
#for_window [class="Skype"] border none
#for_window [class="VirtualBox"] border none
#for_window [class="Gnome-terminal"] border none

# Border windows
for_window [instance="vimNotes"] border 1pixel
#for_window [class="Gnome-terminal" class="vimNotes"] border 1pixel
for_window [class="Keepassx"] border 1pixel

#
# Modes
#
# Resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"
mode "resize" {
    bindsym h resize shrink width 10 px or 10 ppt
    bindsym j resize grow height 10 px or 10 ppt
    bindsym k resize shrink height 10 px or 10 ppt
    bindsym l resize grow width 10 px or 10 ppt

    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Power menu
bindsym XF86PowerOff mode "$mode_system"

set $mode_system (l) lock, (o) logout, (s) suspend, (r) reboot, (p) poweroff

mode "$mode_system" {
	bindsym l exec --no-startup-id ~/.i3/i3exit.sh lock, mode "default"
	bindsym o exec --no-startup-id ~/.i3/i3exit.sh logout, mode "default"
	bindsym s exec --no-startup-id ~/.i3/i3exit.sh suspend, mode "default"
	bindsym r exec --no-startup-id ~/.i3/i3exit.sh reboot, mode "default"
	bindsym p exec --no-startup-id ~/.i3/i3exit.sh poweroff, mode "default"

	bindsym Return mode "default"
	bindsym Escape mode "default"
}

# Kill focused window menu
bindsym $mod+Shift+q mode "$mode_kill_window"

set $mode_kill_window Kill window: (y) yes, (n) no

mode "$mode_kill_window" {
    bindsym y kill; mode "default"

    bindsym n mode "default"
	bindsym Escape mode "default"
}
